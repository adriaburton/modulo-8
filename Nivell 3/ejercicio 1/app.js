var nombre = ["A", "D", "R", "I", "A", "N"];
for (let index = 0; index < nombre.length; index++) {
  console.log(nombre[index]);

  var firstInput = nombre;

  document.getElementById("result").innerHTML = firstInput;
}
